__pycache__/

# Local PAT for dashboard-push helper. Mirror of ~/.claude/dashboard-push-token,
# placed inside the repo so Cowork's sandbox can read it (Cowork doesn't mount ~/.claude/).
.dashboard-push-token
